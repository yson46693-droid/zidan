# 🔗 دليل الربط الرمزي (Symbolic Link) - حل مشكلة open_basedir

## 📋 نظرة عامة

إذا كان مشروعك موجوداً في مجلد `zidan` خارج `httpdocs` (المجلد الرئيسي للموقع)، فأنت بحاجة إلى إنشاء **ربط رمزي** (Symbolic Link) لربط المجلد بالموقع.

## 🎯 الهدف

ربط مجلد المشروع (`zidan`) بمجلد `httpdocs` أو `public_html` بحيث يكون المشروع متاحاً عبر الويب مع الحفاظ على الملفات في موقعها الأصلي.

## 📍 هيكل المجلدات على الخادم

```
/var/www/vhosts/egsystem.top/
├── httpdocs/              ← المجلد الرئيسي للموقع (WEBSPACEROOT)
│   └── (ملفات الموقع هنا)
└── zidan/                 ← مجلد المشروع (خارج httpdocs)
    ├── api/
    ├── css/
    ├── js/
    └── index.html
```

**بعد الربط الرمزي:**
```
/var/www/vhosts/egsystem.top/
├── httpdocs/
│   └── zidan -> /var/www/vhosts/egsystem.top/zidan  ← رابط رمزي
└── zidan/                 ← الملفات الفعلية هنا
    ├── api/
    ├── css/
    └── index.html
```

## 🚀 خطوات التنفيذ

### الطريقة 1: عبر SSH (الأفضل)

### الخطوة 1: الاتصال بالخادم عبر SSH

```bash
# الاتصال بالخادم
ssh username@your-server.com

# أو إذا كان لديك Plesk
ssh username@egsystem.top -p 22
```

### الخطوة 2: الانتقال إلى المجلد الرئيسي

```bash
# الانتقال إلى مجلد الموقع
cd /var/www/vhosts/egsystem.top/

# التحقق من وجود المجلدات
ls -la
# يجب أن ترى:
# - httpdocs/ (أو public_html/)
# - zidan/
```

### الخطوة 3: إنشاء الربط الرمزي

```bash
# الانتقال إلى مجلد httpdocs
cd httpdocs

# إنشاء رابط رمزي من httpdocs/zidan إلى المجلد الأصلي
ln -s /var/www/vhosts/egsystem.top/zidan zidan

# التحقق من نجاح العملية
ls -la
# يجب أن ترى:
# lrwxrwxrwx 1 user user 45 Jan  1 12:00 zidan -> /var/www/vhosts/egsystem.top/zidan
```

### الخطوة 4: التحقق من الصلاحيات

```bash
# التأكد من أن المجلد الأصلي لديه الصلاحيات الصحيحة
chmod 755 /var/www/vhosts/egsystem.top/zidan
chmod -R 755 /var/www/vhosts/egsystem.top/zidan/*

# التأكد من أن المالك صحيح
chown -R username:username /var/www/vhosts/egsystem.top/zidan
```

### الخطوة 5: اختبار الربط

```bash
# التحقق من أن الملفات متاحة عبر الربط
ls -la httpdocs/zidan/
# يجب أن ترى ملفات المشروع

# اختبار الوصول إلى ملف
cat httpdocs/zidan/index.html
```

---

## 🌐 الطريقة 2: عبر Plesk Panel

### الخطوة 1: فتح Plesk Panel

1. افتح `https://egsystem.top:8443` (أو عنوان Plesk الخاص بك)
2. سجل الدخول بحسابك

### الخطوة 2: الوصول إلى File Manager

1. اختر الدومين `egsystem.top`
2. اذهب إلى **Files** → **File Manager**

### الخطوة 3: إنشاء الربط الرمزي

1. انتقل إلى مجلد `httpdocs` (أو `public_html`)
2. انقر على **New** → **Symbolic Link**
3. أدخل:
   - **Link Name**: `zidan`
   - **Target Path**: `/var/www/vhosts/egsystem.top/zidan`
4. انقر **OK**

### الخطوة 4: التحقق

1. تأكد من ظهور مجلد `zidan` في `httpdocs`
2. افتح المجلد للتحقق من الملفات

---

## 🌐 الطريقة 3: عبر cPanel (إذا كان متاحاً)

### الخطوة 1: فتح cPanel

1. افتح `https://egsystem.top:2083`
2. سجل الدخول

### الخطوة 2: استخدام File Manager

1. اذهب إلى **Files** → **File Manager**
2. انتقل إلى `public_html`
3. انقر **+ Symbolic Link** (أو من القائمة)
4. أدخل:
   - **Link Name**: `zidan`
   - **Target**: `/var/www/vhosts/egsystem.top/zidan`

---

## 🔧 الطريقة 4: عبر FTP/SFTP (إذا لم يكن SSH متاحاً)

**ملاحظة**: معظم عملاء FTP لا يدعمون إنشاء روابط رمزية مباشرة. استخدم SSH أو لوحة التحكم.

---

## ✅ التحقق من نجاح العملية

### 1. التحقق من وجود الربط

```bash
# عبر SSH
ls -la /var/www/vhosts/egsystem.top/httpdocs/ | grep zidan
# يجب أن ترى: lrwxrwxrwx ... zidan -> /var/www/vhosts/egsystem.top/zidan
```

### 2. اختبار الوصول عبر المتصفح

افتح في المتصفح:
- `https://zidan.egsystem.top/` (إذا كان subdomain)
- `https://egsystem.top/zidan/` (إذا كان subdirectory)

### 3. التحقق من ملفات الجلسات

```bash
# التحقق من أن الجلسات تُحفظ في /tmp
ls -la /tmp/ | grep sess_
```

### 4. فحص سجلات الأخطاء

```bash
# التحقق من عدم وجود أخطاء open_basedir
tail -f /var/www/vhosts/egsystem.top/zidan/logs/php_errors.log
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "Permission denied" عند إنشاء الربط

**الحل:**
```bash
# التأكد من الصلاحيات
chmod 755 /var/www/vhosts/egsystem.top/zidan
chown username:username /var/www/vhosts/egsystem.top/zidan
```

### المشكلة 2: الربط لا يعمل (404 Not Found)

**الحل:**
```bash
# التحقق من المسار
ls -la /var/www/vhosts/egsystem.top/httpdocs/zidan

# إعادة إنشاء الربط
rm /var/www/vhosts/egsystem.top/httpdocs/zidan
ln -s /var/www/vhosts/egsystem.top/zidan /var/www/vhosts/egsystem.top/httpdocs/zidan
```

### المشكلة 3: "open_basedir restriction" لا يزال موجوداً

**الحل:**
1. تأكد من أن `config.php` تم تحديثه
2. امسح ملفات الجلسات القديمة:
   ```bash
   rm -f /tmp/sess_*
   ```
3. أعد تشغيل Apache/PHP-FPM:
   ```bash
   service apache2 restart
   # أو
   service php-fpm restart
   ```

### المشكلة 4: الملفات غير مرئية عبر الويب

**الحل:**
```bash
# التأكد من الصلاحيات
chmod -R 755 /var/www/vhosts/egsystem.top/zidan
chown -R username:username /var/www/vhosts/egsystem.top/zidan

# التأكد من أن Apache يمكنه قراءة الملفات
chmod 644 /var/www/vhosts/egsystem.top/zidan/*.html
chmod 644 /var/www/vhosts/egsystem.top/zidan/*.php
```

---

## 📝 ملاحظات مهمة

### ✅ المزايا

1. **المرونة**: الملفات تبقى في موقعها الأصلي
2. **الأمان**: يمكنك وضع الملفات الحساسة خارج `httpdocs`
3. **سهولة الصيانة**: تحديث واحد يؤثر على الموقع

### ⚠️ التحذيرات

1. **لا تحذف المجلد الأصلي**: حذف `/var/www/vhosts/egsystem.top/zidan` سيكسر الربط
2. **الصلاحيات**: تأكد من أن الصلاحيات صحيحة
3. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية قبل التعديل

---

## 🔄 إزالة الربط الرمزي

إذا أردت إزالة الربط:

```bash
# حذف الربط (لا يحذف الملفات الأصلية)
rm /var/www/vhosts/egsystem.top/httpdocs/zidan

# أو نقل الملفات مباشرة إلى httpdocs
mv /var/www/vhosts/egsystem.top/zidan/* /var/www/vhosts/egsystem.top/httpdocs/
```

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من سجلات الأخطاء: `logs/php_errors.log`
2. تحقق من صلاحيات المجلدات
3. تأكد من أن `open_basedir` يشمل `/tmp` و `WEBSPACEROOT`

---

## ✅ قائمة التحقق النهائية

- [ ] تم إنشاء الربط الرمزي بنجاح
- [ ] الملفات مرئية عبر المتصفح
- [ ] لا توجد أخطاء `open_basedir` في السجلات
- [ ] الجلسات تعمل بشكل صحيح
- [ ] الصلاحيات صحيحة
- [ ] تم اختبار تسجيل الدخول

---

**تاريخ الإنشاء**: 2026-01-01  
**آخر تحديث**: 2026-01-01
