# نظام إدارة الإصدارات
## Version Management System

## 📋 نظرة عامة

نظام إدارة الإصدارات التلقائي يحدّث رقم الإصدار في جميع الملفات تلقائياً عند كل تعديل.

## 🚀 الاستخدام

### تحديث الإصدار تلقائياً

**الطريقة 1: استخدام PowerShell**
```powershell
.\update-version.ps1
```

**الطريقة 2: استخدام Batch (أسهل)**
```batch
update-version.bat
```

### ما الذي يتم تحديثه؟

عند تشغيل السكريبت، يتم تحديث:
- ✅ `version.json` - الملف المركزي للإصدار
- ✅ `manifest.json` - إصدار PWA
- ✅ `js/version.js` - ملف JavaScript للإصدار
- ✅ `sw.js` - Service Worker
- ✅ جميع ملفات HTML (index.html, dashboard.html, pos.html, etc.)

## 📍 أماكن عرض الإصدار

يظهر رقم الإصدار في:
1. **الشريط الجانبي** (sidebar-footer) في dashboard.html
2. **الـ header** (top-bar) في dashboard.html
3. **صفحة تسجيل الدخول** (index.html)

## 🔧 كيفية عمل النظام

1. **ملف مركزي**: `version.json` يحتوي على رقم الإصدار الحالي
2. **تحديث تلقائي**: السكريبت يزيد رقم الإصدار تلقائياً (patch → minor → major)
3. **عرض في الواجهة**: JavaScript يقرأ الإصدار ويعرضه تلقائياً

## 📝 تنسيق الإصدار

الإصدار يتبع تنسيق: `MAJOR.MINOR.PATCH`
- **MAJOR**: تحديثات كبيرة (تغييرات غير متوافقة)
- **MINOR**: ميزات جديدة (متوافقة مع الإصدارات السابقة)
- **PATCH**: إصلاحات أخطاء (متوافقة تماماً)

## ⚙️ الإعدادات

### تغيير رقم الإصدار الافتراضي

عدّل `version.json`:
```json
{
  "version": "2.0.1",
  "build": 1,
  "last_updated": "2024-01-01T00:00:00Z"
}
```

### إضافة Git Hook (اختياري)

للتحديث التلقائي قبل كل commit، أضف في `.git/hooks/pre-commit`:
```bash
#!/bin/sh
powershell.exe -ExecutionPolicy Bypass -File update-version.ps1
git add version.json manifest.json js/version.js sw.js
```

## 📌 ملاحظات

- السكريبت يزيد رقم PATCH تلقائياً عند كل تشغيل
- عند وصول PATCH إلى 10، يزيد MINOR تلقائياً
- عند وصول MINOR إلى 10، يزيد MAJOR تلقائياً
- رقم الإصدار يظهر تلقائياً في الواجهة بعد تحميل `version.js`

## 🎯 مثال

```bash
# قبل التحديث
version.json: "version": "2.0.1"

# بعد تشغيل update-version.ps1
version.json: "version": "2.0.2"
```

---

**تم إنشاء النظام بنجاح! 🎉**

