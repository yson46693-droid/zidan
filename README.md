# 📱 نظام إدارة محل صيانة الهواتف المحمولة

نظام ويب شامل مصمم لإدارة عمليات صيانة الهواتف المحمولة، مبني باستخدام HTML5, CSS3, JavaScript مع دعم كامل للغة العربية واتجاه RTL.

## ✨ المميزات

### 🔐 نظام المصادقة والصلاحيات
- تسجيل دخول آمن
- 3 مستويات صلاحيات: **مالك** (admin)، **مدير** (manager)، **موظف** (employee)
- إدارة كاملة للمستخدمين

### 🏠 لوحة التحكم
- إحصائيات يومية شاملة
- عرض العمليات الحالية
- واجهة عربية سهلة الاستخدام

### 🔧 إدارة عمليات الصيانة
- إضافة وتعديل عمليات الصيانة
- تتبع حالة كل عملية (قيد الانتظار، قيد الإصلاح، جاهز، تم التسليم، ملغي)
- طباعة إيصالات الاستلام والتسليم
- رقم عملية فريد لكل إصلاح

### 👥 إدارة العملاء
- قاعدة بيانات العملاء
- سجل كامل لعمليات كل عميل
- بحث سريع عن العملاء

### 📦 إدارة المخزون
- تتبع قطع الغيار
- تنبيهات للكميات المنخفضة
- إدارة أسعار الشراء والبيع

### 💰 إدارة المصروفات
- تسجيل جميع المصروفات
- تصنيف المصروفات (إيجار، كهرباء، رواتب، قطع غيار، أخرى)
- فلترة حسب النوع والتاريخ

### 📊 التقارير المالية
- تقارير يومية وشهرية ومخصصة
- حساب الإيرادات والمصروفات وصافي الربح
- طباعة التقارير

### 🧾 الفواتير الإلكترونية ⭐ جديد
- إنشاء فواتير احترافية
- حساب تلقائي للضريبة والخصم
- عناصر متعددة في الفاتورة
- طباعة فواتير بتصميم احترافي
- سجل كامل للفواتير
- حالات الفاتورة (مدفوعة، غير مدفوعة، ملغاة)

### ⚙️ الإعدادات
- إعدادات المحل الأساسية
- إدارة المستخدمين
- نسخ احتياطي واستعادة البيانات
- الوضع الليلي/النهاري

### 📱 تطبيق PWA ⭐ جديد
- تثبيت كتطبيق على الهاتف والكمبيوتر
- العمل بدون إنترنت (Offline)
- سرعة فائقة في التحميل
- واجهة تطبيق مستقل
- جاهز للإشعارات Push

## 🛠️ التقنيات المستخدمة

- **Frontend**: HTML5, CSS3, JavaScript (Vanilla JS)
- **Backend**: PHP 7.4+
- **قاعدة البيانات**: MySQL 5.7+ / MariaDB 10.2+
- **التصميم**: RTL responsive design
- **الأمان**: Password hashing, session management, CSRF protection
- **المزامنة**: Auto-sync كل 30 ثانية بين الأجهزة
- **الدولة**: مصر 🇪🇬
- **العملة**: الجنيه المصري (ج.م)

## 📋 المتطلبات

- خادم ويب (Apache/Nginx)
- PHP 7.4 أو أحدث مع إضافة MySQLi
- MySQL 5.7+ أو MariaDB 10.2+
- دعم `.htaccess` (لحماية مجلد البيانات)

## 🚀 التثبيت

1. **رفع الملفات**
   ```bash
   # رفع جميع الملفات إلى مجلد الخادم
   ```

2. **إعداد قاعدة البيانات MySQL**
   
   أ. إنشاء قاعدة البيانات:
   ```bash
   # قم بتسجيل الدخول إلى MySQL
   mysql -u root -p
   
   # أو استخدم phpMyAdmin لتنفيذ ملف database.sql
   ```
   
   ب. تنفيذ ملف SQL:
   ```sql
   -- قم بتنفيذ ملف database.sql الموجود في المجلد الرئيسي
   -- أو قم بإنشاء قاعدة البيانات يدوياً:
   CREATE DATABASE mobile_repair_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
   
   ج. تعديل إعدادات الاتصال:
   ```php
   // افتح ملف api/database.php وعدّل الإعدادات:
   define('DB_HOST', 'localhost');      // عنوان الخادم
   define('DB_USER', 'root');            // اسم المستخدم
   define('DB_PASS', '');                // كلمة المرور
   define('DB_NAME', 'mobile_repair_shop'); // اسم قاعدة البيانات
   ```

3. **إعداد الصلاحيات**
   ```bash
   # التأكد من أن مجلد data قابل للكتابة (للنسخ الاحتياطي)
   chmod 755 data
   chmod 755 backups
   ```

4. **الوصول للنظام**
   - افتح المتصفح وانتقل إلى عنوان النظام
   - ستظهر صفحة تسجيل الدخول
   - سيتم إنشاء المستخدم الافتراضي تلقائياً عند أول تشغيل

5. **بيانات الدخول الافتراضية**
   - **اسم المستخدم**: `admin`
   - **كلمة المرور**: `admin123`
   
   ⚠️ **مهم**: قم بتغيير كلمة المرور الافتراضية فوراً من الإعدادات!

## 📁 هيكل المشروع

```
/
├── index.html              # صفحة تسجيل الدخول
├── dashboard.html          # لوحة التحكم الرئيسية
├── css/
│   ├── style.css          # التنسيقات الأساسية + RTL
│   ├── dark-mode.css      # الوضع الليلي
│   └── print.css          # تنسيقات الطباعة
├── js/
│   ├── auth.js            # المصادقة والصلاحيات
│   ├── api.js             # التواصل مع PHP API
│   ├── repairs.js         # إدارة عمليات الصيانة
│   ├── customers.js       # إدارة العملاء
│   ├── inventory.js       # إدارة المخزون
│   ├── expenses.js        # إدارة المصروفات
│   ├── reports.js         # التقارير المالية
│   ├── settings.js        # الإعدادات
│   └── utils.js           # دوال مساعدة
├── api/
│   ├── config.php         # إعدادات النظام
│   ├── database.php        # إعدادات قاعدة البيانات MySQL
│   ├── auth.php           # API المصادقة
│   ├── users.php          # API المستخدمين
│   ├── repairs.php        # API عمليات الصيانة
│   ├── customers.php      # API العملاء
│   ├── inventory.php      # API المخزون
│   ├── expenses.php       # API المصروفات
│   ├── reports.php        # API التقارير
│   └── settings.php       # API الإعدادات
├── database.sql           # ملف إنشاء قاعدة البيانات
└── data/
    ├── .htaccess          # حماية المجلد
    └── (ملفات JSON للنسخ الاحتياطي فقط)
```

## 🔒 الأمان

- ✅ تشفير كلمات المرور باستخدام `password_hash()`
- ✅ حماية مجلد البيانات من الوصول المباشر
- ✅ التحقق من الصلاحيات في كل طلب API
- ✅ Prepared Statements لمنع SQL Injection
- ✅ Session management آمن
- ✅ تنظيف البيانات قبل الإدخال في قاعدة البيانات

## 🔄 النسخ الاحتياطي

يمكنك إنشاء نسخة احتياطية من جميع بياناتك:

1. اذهب إلى **الإعدادات**
2. اضغط على **تصدير نسخة احتياطية**
3. سيتم تنزيل ملف JSON يحتوي على جميع البيانات

**استعادة النسخة الاحتياطية:**
1. اذهب إلى **الإعدادات**
2. اضغط على **استعادة نسخة احتياطية**
3. اختر ملف JSON المحفوظ مسبقاً

## 👥 الصلاحيات

### مالك (Admin)
- وصول كامل لجميع الميزات
- إدارة المستخدمين
- الإعدادات
- النسخ الاحتياطي

### مدير (Manager)
- إدارة العمليات والعملاء
- إدارة المخزون والمصروفات
- عرض التقارير المالية
- لا يمكنه الوصول للإعدادات أو إدارة المستخدمين

### موظف (Employee)
- إضافة عمليات صيانة جديدة
- إضافة عملاء جدد
- عرض العمليات والعملاء
- لا يمكنه الحذف أو الوصول للمصروفات والتقارير

## 📱 الاستجابة

النظام مصمم ليعمل بكفاءة على:
- 💻 أجهزة الكمبيوتر
- 📱 الهواتف الذكية
- 📱 الأجهزة اللوحية

## 🖨️ الطباعة

يدعم النظام طباعة:
- إيصالات استلام الأجهزة
- إيصالات تسليم الأجهزة
- الفواتير الإلكترونية
- التقارير المالية

## 📱 Progressive Web App (PWA)

النظام يعمل كتطبيق موبايل كامل:
- ✅ تثبيت على الشاشة الرئيسية (Android, iOS, Desktop)
- ✅ يعمل بدون إنترنت (Offline Mode)
- ✅ سرعة فائقة (Instant Loading)
- ✅ واجهة مستقلة (بدون شريط المتصفح)
- ✅ حجم صغير (~200 KB)
- ✅ جاهز للإشعارات

راجع `PWA_GUIDE.md` للمزيد من التفاصيل.

## 🌙 الوضع الليلي

يدعم النظام الوضع الليلي لراحة العين أثناء العمل الليلي.

## 🐛 الإبلاغ عن المشاكل

إذا واجهت أي مشاكل، يرجى:
1. التحقق من صلاحيات مجلد `data`
2. التأكد من تفعيل PHP
3. مراجعة سجلات الأخطاء في الخادم

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

## 🙏 شكر وتقدير

شكراً لاستخدامك نظام إدارة محل صيانة الهواتف!

---

**تم التطوير بواسطة**: AI Assistant  
**الإصدار**: 1.0.0  
**تاريخ الإصدار**: 2025

