# ⚡ حل سريع لمشاكل LiteSpeed + PHP

## 🎯 المشاكل
- ❌ 403 Forbidden على الصفحة الرئيسية
- ❌ 404 Not Found على ملفات موجودة
- ❌ open_basedir restriction
- ❌ خطأ ErrorDocument 403

## ✅ الحلول المطبقة

### 1. تم تحديث `.htaccess`
- ✅ إضافة `DirectoryIndex`
- ✅ إضافة قواعد السماح للملفات
- ✅ إصلاح `ErrorDocument 403`
- ✅ إضافة قواعد `RewriteEngine` للملفات الموجودة

### 2. تم إنشاء `403.html`
- ✅ صفحة خطأ مخصصة لـ 403 Forbidden

### 3. تم إنشاء `test-session.php`
- ✅ ملف اختبار للتحقق من إعدادات Session و open_basedir

### 4. تم تحديث `api/config.php` (موجود مسبقاً)
- ✅ `session.save_path` مضبوط على `/tmp`
- ✅ `soap.wsdl_cache_enabled` معطّل

## 📋 خطوات التحقق

### 1. تحقق من الصفحة الرئيسية
```
https://alaazidan.store/
```
يجب أن تظهر صفحة تسجيل الدخول بدون خطأ 403.

### 2. تحقق من ملفات موجودة
```
https://alaazidan.store/css/style.css
https://alaazidan.store/js/api.js
```
يجب أن يتم تحميل الملفات بدون خطأ 404.

### 3. تحقق من Session Settings
```
https://alaazidan.store/test-session.php
```
يجب أن تظهر جميع الاختبارات ✅ (نجاح).

### 4. تحقق من صفحة الخطأ 403
```
https://alaazidan.store/403.html
```
يجب أن تظهر صفحة خطأ مخصصة.

## 🔧 إذا استمرت المشاكل

### مشكلة 403 مستمرة:
1. تحقق من الصلاحيات:
   ```bash
   chmod 644 index.html
   chmod 755 .
   ```

2. تحقق من `.htaccess`:
   ```bash
   # تعطيل مؤقتاً للاختبار
   mv .htaccess .htaccess.bak
   ```

### مشكلة open_basedir مستمرة:
1. راجع `api/config.php` - الكود موجود في السطور 148-184
2. إذا لم يعمل، اتصل بالدعم الفني لطلب تعديل `open_basedir`

## 📚 للمزيد من التفاصيل
راجع ملف `LITESPEED_FIX_GUIDE.md` للحلول الكاملة والتفصيلية.

## ⚠️ ملاحظات أمنية
- احذف `test-session.php` بعد التحقق من الإعدادات
- لا تترك ملفات الاختبار في الإنتاج
