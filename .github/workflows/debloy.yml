name: Deploy to Thamara Cloud

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 213.239.220.209
          username: osama74
          password: QEvhhbM52d1?kgt#
          protocol: ftp
          port: 21
          security: loose
          timeout: 120000

          local-dir: public_html/
          server-dir: /httpdocs/z/

          dangerous-clean-slate: false

          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/.env*
            **/.vscode/**
            **/.idea/**
            **/*.log
            **/.DS_Store
