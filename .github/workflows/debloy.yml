name: Deploy to Thamara Cloud

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify Icons Directory
        run: |
          echo "🔍 التحقق من وجود ملفات الأيقونات..."
          if [ ! -d "icons" ]; then
            echo "❌ مجلد icons غير موجود!"
            exit 1
          fi
          icon_count=$(find icons -name "*.png" | wc -l)
          echo "✅ تم العثور على $icon_count ملف أيقونة"
          ls -la icons/
          echo ""
          echo "📋 قائمة الملفات في مجلد icons:"
          find icons -type f -name "*.png" | sort

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 213.239.220.209
          username: alaazidan
          password: KGw~1blLgk1hz*d1
          server-dir: /httpdocs/

          protocol: ftp
          port: 21
          security: loose
          timeout: 120000
          log-level: verbose


          # مهم: لا يحذف ملفات السيرفر 
          dangerous-clean-slate: false

          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.env
            **/.env.*
            **/.vscode/**
            **/.idea/**
            **/README.md
            **/*.log
            **/backups/**
            **/temp/**
            **/tmp/**
            **/.DS_Store
            **/Thumbs.db
            .gitignore
            .editorconfig

      - name: Upload Icons Directory Explicitly
        run: |
          echo "📤 رفع مجلد icons بشكل صريح..."
          # تثبيت lftp
          sudo apt-get update -qq
          sudo apt-get install -y lftp
          
          # إنشاء ملف script لـ lftp لتجنب مشاكل escape
          cat > /tmp/lftp_upload.sh << 'EOF'
          set ftp:ssl-allow no
          set ftp:passive-mode yes
          set ftp:list-options -a
          open -u alaazidan,KGw~1blLgk1hz*d1 ftp://213.239.220.209:21
          cd /httpdocs/
          mkdir -p icons
          cd icons
          lcd icons
          mput *.png
          put .htaccess
          ls -la
          quit
          EOF
          
          # تنفيذ script
          lftp -f /tmp/lftp_upload.sh
          echo "✅ تم رفع ملفات الأيقونات و .htaccess"

      - name: Verify Icons Upload
        run: |
          echo "🔍 التحقق من رفع ملفات الأيقونات..."
          echo "⚠️ يرجى التحقق يدوياً من وجود مجلد icons/ على السيرفر في /httpdocs/icons/"
          echo "📋 يجب أن يحتوي المجلد على الملفات التالية:"
          echo "   - icon-72x72.png"
          echo "   - icon-96x96.png"
          echo "   - icon-128x128.png"
          echo "   - icon-144x144.png"
          echo "   - icon-152x152.png"
          echo "   - icon-192x192.png"
          echo "   - icon-384x384.png"
          echo "   - icon-512x512.png"
