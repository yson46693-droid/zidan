name: Deploy to Thamara Cloud

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0


      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.alaazidan.online
          username: alaa
          password: A8r9yNP!#vz@E4V
          server-dir: /public_html/
          protocol: ftp
          port: 21
          security: loose
          timeout: 120000
          log-level: verbose


          # مهم: لا يحذف ملفات السيرفر  
          dangerous-clean-slate: false

          exclude: |
            **/icons/**
            **/sessions/**
            **/data/**
            **/logs/**
            **/backups/**
            **/temp/**
            **/tmp/**
            **/.DS_Store
            **/Thumbs.db
            .gitignore
            .editorconfig
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.env
            **/.env.*
            **/.vscode/**
            **/.idea/**
            **/README.md
            **/*.log
            **/backups/**
            **/temp/**
            **/tmp/**
            **/.DS_Store
            **/Thumbs.db
            .gitignore
            .editorconfig