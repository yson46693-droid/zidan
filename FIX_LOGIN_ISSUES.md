# 🔧 دليل إصلاح مشاكل تسجيل الدخول - الموقع الثاني

## 📋 المشاكل المكتشفة

### 1️⃣ أخطاء 403 Forbidden (صلاحيات الملفات)

**الأعراض:**
- جميع ملفات JS و CSS تحصل على خطأ 403
- الملفات موجودة لكن لا يمكن الوصول إليها

**الحل:**

#### على السيرفر (SSH):
```bash
# الاتصال بالسيرفر
ssh user@alaazidan.store

# الانتقال لمجلد الموقع
cd /path/to/website

# تطبيق الصلاحيات
chmod 755 js/ css/ api/ webauthn/ icons/
chmod 644 js/*.js css/*.css

# أو استخدام السكريبت
bash fix-permissions.sh
```

#### عبر FileZilla:
1. افتح FileZilla
2. اتصل بالسيرفر
3. انقر بالزر الأيمن على مجلد `js/` → File permissions → 755
4. انقر بالزر الأيمن على مجلد `css/` → File permissions → 755
5. لكل ملف JS: File permissions → 644
6. لكل ملف CSS: File permissions → 644

---

### 2️⃣ Service Worker Error (MIME Type)

**الأعراض:**
```
Failed to register a ServiceWorker: 
The script has an unsupported MIME type ('text/html').
```

**الحل:**

#### الخطوة 1: اختبار sw.js.php
افتح في المتصفح:
```
https://www.alaazidan.store/sw.js.php
```

**يجب أن ترى:**
- ✅ محتوى JavaScript (كود Service Worker)
- ✅ Content-Type: `application/javascript`

**إذا رأيت:**
- ❌ HTML أو رسالة خطأ → المشكلة في إعدادات السيرفر

#### الخطوة 2: التحقق من .htaccess
تأكد من وجود ملف `.htaccess` في الجذر مع الإعدادات الصحيحة (تم تحديثه تلقائياً)

#### الخطوة 3: التحقق من إعدادات PHP
تأكد من أن السيرفر ينفذ PHP للملفات `.php`

#### الخطوة 4: استخدام ملف اختبار
افتح في المتصفح:
```
https://www.alaazidan.store/test-sw-js.php
```

سيعرض لك تقريراً مفصلاً عن حالة Service Worker

---

### 3️⃣ ملفات مفقودة (404)

**الملفات المفقودة:**
- `/icons/icon-512x512.png`
- `/webauthn/webauthn.js` (اختياري)

**الحل:**

#### لـ icon-512x512.png:
```bash
# إذا كان الملف موجود في الجذر
cp icon-512x512.png icons/icon-512x512.png
chmod 644 icons/icon-512x512.png
```

#### لـ webauthn.js:
- إذا كان الملف موجود في المشروع، انسخه إلى `webauthn/`
- إذا لم يكن موجوداً، يمكن إزالته من `index.html` (غير حرج)

---

## ✅ قائمة التحقق السريعة

قبل إعادة المحاولة:

- [ ] ✅ جميع ملفات JS لها صلاحيات 644
- [ ] ✅ جميع ملفات CSS لها صلاحيات 644
- [ ] ✅ جميع المجلدات (js/, css/) لها صلاحيات 755
- [ ] ✅ ملف `.htaccess` موجود ومحدث
- [ ] ✅ ملف `sw.js.php` موجود وصلاحياته 644
- [ ] ✅ ملف `sw.js.php` يعيد JavaScript (اختبار يدوي)
- [ ] ✅ ملف `icon-512x512.png` موجود في `icons/`
- [ ] ✅ جميع الملفات المطلوبة موجودة على السيرفر

---

## 🧪 اختبار بعد الإصلاح

1. افتح https://www.alaazidan.store/index.html
2. افتح Developer Tools (F12)
3. اذهب إلى Console و Network
4. حاول تسجيل الدخول (username=1, password=1)
5. تحقق من:
   - ✅ لا توجد أخطاء 403
   - ✅ لا توجد أخطاء 404 (إلا للملفات الاختيارية)
   - ✅ Service Worker مسجل بنجاح
   - ✅ جميع الملفات الحرجة تم تحميلها
   - ✅ تسجيل الدخول يعمل

---

## 📝 ملاحظات

### أخطاء غير حرجة (يمكن تجاهلها مؤقتاً):
- ⚠️ `Failed to load PWA Install Manager` - PWA اختياري
- ⚠️ `Failed to load PWA Validator` - PWA اختياري
- ⚠️ `Failed to load WebAuthn script` - WebAuthn اختياري

### أخطاء حرجة (يجب إصلاحها):
- 🔴 أخطاء 403 على ملفات JS/CSS - **يجب إصلاحها فوراً**
- 🔴 Service Worker Error - **يجب إصلاحها فوراً**
- 🔴 فشل تحميل `js/auth.js` - **يجب إصلاحها فوراً**

---

## 🆘 إذا استمرت المشكلة

1. **تحقق من سجلات الأخطاء:**
   ```bash
   tail -f /path/to/logs/error.log
   ```

2. **تحقق من صلاحيات الملفات:**
   ```bash
   ls -la js/ css/
   ```

3. **تحقق من إعدادات PHP:**
   ```bash
   php -i | grep "display_errors"
   ```

4. **اتصل بالدعم الفني للاستضافة** إذا كانت المشكلة في إعدادات السيرفر

---

**آخر تحديث:** 2024-02-01  
**الإصدار:** 1.0
