# 📋 دليل صلاحيات الملفات والمجلدات (CHMOD)

## 🎯 الصلاحيات الصحيحة للموقع

### ✅ الصلاحيات الأساسية الموصى بها

```bash
# 📁 المجلدات (Directories)
# يجب أن تكون 755 (rwxr-xr-x)
# المالك: قراءة/كتابة/تنفيذ | المجموعة: قراءة/تنفيذ | الآخرون: قراءة/تنفيذ

# 📄 الملفات العادية (Files)
# يجب أن تكون 644 (rw-r--r--)
# المالك: قراءة/كتابة | المجموعة: قراءة | الآخرون: قراءة
```

---

## 📋 الصلاحيات حسب نوع الملف

### 1️⃣ المجلد الرئيسي والجذر

```bash
# المجلد الرئيسي
chmod 755 .

# المجلدات الفرعية
chmod 755 api/
chmod 755 css/
chmod 755 js/
chmod 755 images/
chmod 755 icons/
chmod 755 avatars/
chmod 755 chat/
chmod 755 backups/
chmod 755 logs/
chmod 755 sessions/
chmod 755 invoices/
chmod 755 data/
chmod 755 includes/
chmod 755 webauthn/
chmod 755 brands/
```

### 2️⃣ ملفات HTML

```bash
# جميع ملفات HTML
find . -type f -name "*.html" -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 index.html
chmod 644 dashboard.html
chmod 644 chat.html
chmod 644 pos.html
chmod 644 repair-tracking.html
chmod 644 install.html
chmod 644 403.html
```

### 3️⃣ ملفات CSS

```bash
# جميع ملفات CSS
find . -type f -name "*.css" -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 css/*.css
```

### 4️⃣ ملفات JavaScript

```bash
# جميع ملفات JS
find . -type f -name "*.js" -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 js/*.js
chmod 644 sw.js
chmod 644 webauthn/*.js
```

### 5️⃣ ملفات PHP

```bash
# جميع ملفات PHP
find . -type f -name "*.php" -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 api/*.php
chmod 644 router.php
chmod 644 sw-handler.php
chmod 644 sw.js.php
chmod 644 test-session.php
chmod 644 webauthn/*.php
```

### 6️⃣ ملفات الصور

```bash
# جميع ملفات الصور
find . -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.gif" -o -name "*.svg" -o -name "*.ico" -o -name "*.webp" \) -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 images/*.jpg
chmod 644 images/*.png
chmod 644 avatars/*.jpg
chmod 644 icons/*.png
chmod 644 brands/*.svg
chmod 644 favicon.ico
chmod 644 icon-512x512.png
chmod 644 chat/images/*
chmod 644 chat/audio/*.webm
```

### 7️⃣ ملفات JSON والتكوين

```bash
# ملفات JSON
find . -type f -name "*.json" -exec chmod 644 {} \;

# أو بشكل فردي:
chmod 644 manifest.json
chmod 644 version.json
chmod 644 data/*.json
chmod 644 browserconfig.xml
```

### 8️⃣ ملفات التكوين والإعدادات

```bash
# ملفات .htaccess
chmod 644 .htaccess
chmod 644 sessions/.htaccess
chmod 644 icons/.htaccess
chmod 644 chat/files/.htaccess
chmod 644 chat/audio/.htaccess
chmod 644 api/chat/.htaccess

# ملفات PHP.ini
chmod 644 php.ini
chmod 644 .user.ini 2>/dev/null || true
```

### 9️⃣ مجلدات الكتابة (Writeable Directories)

```bash
# ✅ مجلد sessions (يجب أن يكون قابل للكتابة)
chmod 755 sessions/
# الملفات داخل sessions (يتم إنشاؤها تلقائياً)
# يجب أن تكون 600 أو 644 (حسب الإعدادات)

# ✅ مجلد logs (يجب أن يكون قابل للكتابة)
chmod 755 logs/
chmod 644 logs/*.log 2>/dev/null || true

# ✅ مجلد backups (يجب أن يكون قابل للكتابة)
chmod 755 backups/

# ✅ مجلد images (لرفع الصور)
chmod 755 images/
chmod 755 images/backup/

# ✅ مجلد avatars (لرفع الصور الشخصية)
chmod 755 avatars/

# ✅ مجلد chat (لرفع الملفات)
chmod 755 chat/
chmod 755 chat/images/
chmod 755 chat/files/
chmod 755 chat/audio/

# ✅ مجلد invoices (لإنشاء الفواتير)
chmod 755 invoices/

# ✅ مجلد data (لحفظ البيانات)
chmod 755 data/
```

---

## 🔧 سكريبت شامل لتطبيق الصلاحيات

### سكريبت Bash (للأنظمة Linux/Mac)

```bash
#!/bin/bash

# ✅ تطبيق الصلاحيات على جميع الملفات والمجلدات

# المجلد الرئيسي
chmod 755 .

# جميع المجلدات → 755
find . -type d -exec chmod 755 {} \;

# جميع الملفات → 644
find . -type f -exec chmod 644 {} \;

# ✅ مجلدات الكتابة (تأكد من الصلاحيات)
chmod 755 sessions/ logs/ backups/ images/ avatars/ chat/ invoices/ data/ 2>/dev/null
chmod 755 chat/images/ chat/files/ chat/audio/ images/backup/ 2>/dev/null

# ✅ ملفات .htaccess
chmod 644 .htaccess 2>/dev/null
find . -name ".htaccess" -type f -exec chmod 644 {} \; 2>/dev/null

# ✅ ملفات php.ini
chmod 644 php.ini .user.ini 2>/dev/null

echo "✅ تم تطبيق الصلاحيات بنجاح!"
```

### سكريبت PowerShell (للأنظمة Windows)

```powershell
# ✅ تطبيق الصلاحيات على Windows
# ملاحظة: Windows لا يدعم chmod مباشرة
# يجب تطبيق الصلاحيات بعد رفع الملفات على الاستضافة

# للاستخدام: قم بتشغيل هذا السكريبت على الخادم Linux بعد رفع الملفات
# أو استخدم FileZilla أو أي FTP client لتغيير الصلاحيات

Write-Host "⚠️ Windows لا يدعم chmod مباشرة"
Write-Host "✅ يجب تطبيق الصلاحيات بعد رفع الملفات على الاستضافة"
Write-Host "✅ استخدم FileZilla أو SSH لتغيير الصلاحيات"
```

---

## 📝 الصلاحيات المطلوبة حسب الموقع

### ✅ الاستضافة المشتركة (Shared Hosting)

| النوع | الصلاحية | الوصف |
|------|----------|-------|
| المجلدات | **755** | جميع المجلدات |
| ملفات HTML/CSS/JS | **644** | جميع ملفات الواجهة |
| ملفات PHP | **644** | جميع ملفات PHP |
| ملفات الصور | **644** | جميع الصور |
| ملفات JSON | **644** | ملفات التكوين |
| .htaccess | **644** | ملفات الإعدادات |
| مجلدات الكتابة | **755** | sessions, logs, backups, images, avatars, chat |

### ✅ VPS/Cloud (إذا كان لديك صلاحيات root)

نفس الصلاحيات كما هو موضح أعلاه.

---

## 🔍 التحقق من الصلاحيات

### عبر SSH

```bash
# عرض صلاحيات ملف معين
ls -l index.html

# عرض صلاحيات مجلد معين
ls -ld api/

# عرض صلاحيات جميع الملفات
ls -la

# البحث عن ملفات بصلاحيات خاطئة
find . -type f -perm -002  # ملفات قابلة للكتابة من الآخرين (غير آمن)
find . -type f ! -perm 644  # ملفات ليست 644
find . -type d ! -perm 755  # مجلدات ليست 755
```

### عبر FileZilla

1. الاتصال بالخادم عبر FTP
2. النقر بالزر الأيمن على الملف/المجلد
3. اختيار **File permissions** أو **Change file attributes**
4. تعديل الصلاحيات حسب الجدول أعلاه

---

## ⚠️ مشاكل شائعة وحلولها

### ❌ خطأ 403 Forbidden

**السبب:** صلاحيات الملف أو المجلد خاطئة

**الحل:**
```bash
# تأكد من صلاحيات المجلد
chmod 755 .

# تأكد من صلاحيات الملف
chmod 644 index.html
```

### ❌ خطأ 500 Internal Server Error

**السبب:** صلاحيات ملف PHP خاطئة أو ملف .htaccess خاطئ

**الحل:**
```bash
# تأكد من صلاحيات ملفات PHP
chmod 644 api/*.php

# تأكد من صلاحيات .htaccess
chmod 644 .htaccess
```

### ❌ لا يمكن حفظ الملفات (Sessions, Logs)

**السبب:** مجلد الكتابة ليس لديه صلاحيات كتابة

**الحل:**
```bash
# تأكد من صلاحيات مجلدات الكتابة
chmod 755 sessions/
chmod 755 logs/
chmod 755 backups/
chmod 755 images/
chmod 755 avatars/
chmod 755 chat/
chmod 755 invoices/
```

### ❌ لا يمكن رفع الصور

**السبب:** مجلد الصور ليس لديه صلاحيات كتابة

**الحل:**
```bash
# تأكد من صلاحيات مجلدات الصور
chmod 755 images/
chmod 755 avatars/
chmod 755 chat/images/
```

---

## ✅ قائمة التحقق (Checklist)

قبل رفع الموقع على الاستضافة، تأكد من:

- [ ] جميع المجلدات → **755**
- [ ] جميع الملفات → **644**
- [ ] ملفات .htaccess → **644**
- [ ] مجلد sessions → **755**
- [ ] مجلد logs → **755**
- [ ] مجلد backups → **755**
- [ ] مجلد images → **755**
- [ ] مجلد avatars → **755**
- [ ] مجلد chat → **755**
- [ ] مجلد invoices → **755**
- [ ] مجلد data → **755**
- [ ] لا توجد ملفات بصلاحيات **777** (غير آمن)
- [ ] لا توجد ملفات بصلاحيات **000** (غير قابل للوصول)

---

## 🎯 ملخص سريع

```bash
# ✅ الصيغة المختصرة:
# المجلدات: 755
# الملفات: 644
# مجلدات الكتابة: 755

# ✅ تطبيق شامل:
find . -type d -exec chmod 755 {} \;
find . -type f -exec chmod 644 {} \;
```

---

## 📚 مراجع إضافية

- [Linux File Permissions](https://www.linux.com/training-tutorials/understanding-linux-file-permissions/)
- [chmod Command](https://www.computerhope.com/unix/uchmod.htm)
- [File Permissions Calculator](https://chmod-calculator.com/)

---

## 🔒 ملاحظات الأمان

1. **لا تستخدم 777 أبداً** - هذا يعطي صلاحيات كاملة للجميع (غير آمن)
2. **لا تستخدم 000** - هذا يمنع الوصول تماماً
3. **استخدم 644 للملفات** - المالك يقرأ/يكتب، الباقي يقرأ فقط
4. **استخدم 755 للمجلدات** - المالك يقرأ/يكتب/ينفذ، الباقي يقرأ/ينفذ
5. **للملفات الحساسة** (مثل database.php) - استخدم 600 (قراءة/كتابة للمالك فقط)

---

**آخر تحديث:** 2024
**الإصدار:** 1.0
