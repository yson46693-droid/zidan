# دليل الانتقال من JSON إلى MySQL

## نظرة عامة

تم تحويل النظام من استخدام ملفات JSON إلى قاعدة بيانات MySQL لتحسين الأداء والقابلية للتوسع.

## خطوات التثبيت

### 1. إعداد قاعدة البيانات

#### أ. إنشاء قاعدة البيانات
```sql
CREATE DATABASE mobile_repair_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### ب. تنفيذ ملف SQL
قم بتنفيذ ملف `database.sql` الموجود في المجلد الرئيسي:
```bash
mysql -u root -p mobile_repair_shop < database.sql
```

أو استخدم phpMyAdmin:
1. افتح phpMyAdmin
2. اختر قاعدة البيانات `mobile_repair_shop`
3. اضغط على "Import"
4. اختر ملف `database.sql`
5. اضغط "Go"

### 2. تعديل إعدادات الاتصال

افتح ملف `api/database.php` وعدّل الإعدادات:

```php
define('DB_HOST', 'localhost');      // عنوان خادم MySQL
define('DB_USER', 'root');            // اسم المستخدم
define('DB_PASS', '');                // كلمة المرور
define('DB_NAME', 'mobile_repair_shop'); // اسم قاعدة البيانات
```

### 3. التحقق من إضافة MySQLi في PHP

تأكد من تفعيل إضافة MySQLi في PHP:

```php
<?php
// تحقق من وجود MySQLi
if (!extension_loaded('mysqli')) {
    die('إضافة MySQLi غير مفعلة. يرجى تفعيلها في ملف php.ini');
}
?>
```

### 4. استيراد البيانات القديمة (إن وجدت)

إذا كان لديك بيانات في ملفات JSON القديمة، يمكنك استيرادها:

#### أ. إنشاء نسخة احتياطية من ملفات JSON
```bash
cp -r data data_backup
```

#### ب. استخدام ميزة النسخ الاحتياطي في النظام
1. سجل الدخول للنظام
2. اذهب إلى الإعدادات
3. اضغط على "تصدير نسخة احتياطية"
4. سيتم تحميل ملف JSON بجميع البيانات

#### ج. استيراد البيانات إلى MySQL
1. اذهب إلى الإعدادات
2. اضغط على "استعادة نسخة احتياطية"
3. اختر ملف JSON المحفوظ
4. سيتم استيراد جميع البيانات إلى MySQL

## الملفات المعدلة

### ملفات جديدة:
- `api/database.php` - ملف إعدادات قاعدة البيانات
- `database.sql` - ملف إنشاء الجداول

### ملفات معدلة:
- `api/config.php` - إزالة دوال JSON، إضافة تهيئة MySQL
- `api/auth.php` - استخدام MySQL بدلاً من JSON
- `api/users.php` - استخدام MySQL
- `api/repairs.php` - استخدام MySQL
- `api/customers.php` - استخدام MySQL
- `api/inventory.php` - استخدام MySQL
- `api/expenses.php` - استخدام MySQL
- `api/loss-operations.php` - استخدام MySQL
- `api/reports.php` - استخدام MySQL
- `api/settings.php` - استخدام MySQL

## هيكل قاعدة البيانات

### الجداول:
1. **users** - المستخدمين
2. **customers** - العملاء
3. **repairs** - عمليات الصيانة
4. **inventory** - المخزون
5. **expenses** - المصروفات
6. **loss_operations** - العمليات الخاسرة
7. **settings** - الإعدادات
8. **telegram_backup_config** - إعدادات النسخ الاحتياطي عبر Telegram

## ملاحظات مهمة

### 1. الأمان
- جميع الاستعلامات تستخدم Prepared Statements لمنع SQL Injection
- كلمات المرور مشفرة باستخدام `password_hash()`

### 2. الأداء
- تم إضافة فهارس (Indexes) على الحقول المستخدمة بكثرة
- استخدام Transactions للعمليات المعقدة

### 3. التوافق
- النظام متوافق مع MySQL 5.7+ و MariaDB 10.2+
- استخدام UTF8MB4 لدعم جميع الأحرف العربية

### 4. النسخ الاحتياطي
- ميزة النسخ الاحتياطي تعمل الآن مع MySQL
- يمكن تصدير واستيراد جميع البيانات

## استكشاف الأخطاء

### خطأ: "خطأ في الاتصال بقاعدة البيانات"
- تحقق من إعدادات الاتصال في `api/database.php`
- تأكد من أن MySQL يعمل
- تحقق من اسم المستخدم وكلمة المرور

### خطأ: "Table doesn't exist"
- تأكد من تنفيذ ملف `database.sql`
- تحقق من اسم قاعدة البيانات في `api/database.php`

### خطأ: "Access denied"
- تحقق من صلاحيات المستخدم في MySQL
- تأكد من أن المستخدم لديه صلاحيات CREATE, INSERT, UPDATE, DELETE

## الدعم

إذا واجهت أي مشاكل، تحقق من:
1. سجلات الأخطاء في `logs/php_errors.log`
2. سجلات MySQL
3. إعدادات PHP (php.ini)

---

**تم التحديث**: تم تحويل النظام بالكامل من JSON إلى MySQL بنجاح! 🎉

